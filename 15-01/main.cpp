/**
 * Escreva um programa que lê um valor n, cria dinamicamente um vetor de n 
 * elementos, e passa esse vetor para uma função que vai ler os seus elementos. 
 * Depois, no programa principal, o vetor preenchido deve ser impresso. 
 * Além disso, antes de finalizar o programa, deve-se liberar a área de memória
 * alocada.
 */ 

#include <iostream>

 int main () {

    return 0;
 }