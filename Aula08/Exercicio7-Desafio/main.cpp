/*
 * Suponha que temos uma sequência S de n elementos, de forma que cada elemento 
 * em S representa um voto diferente para líder estudantil, dado como um inteiro 
 * representando o número de matrícula do candidato escolhido. Sem assumir 
 * hipóteses sobre os candidatos ou o número de candidatos, implemente:
 *   a) uma função para determinar quem vence a votação representada por S
 *   supondo que o candidato com o maior número de votos será o escolhido.
 *   b) uma função para imprimir a lista completa dos candidatos, por ordem de
 *   número de votos recebidos.
 */
  
#include <iostream>

int main () {

  return 0;
}