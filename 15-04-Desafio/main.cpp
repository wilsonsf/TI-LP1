/**
 * Escreva um programa que monte uma lista de números inteiros positivos. 
 * Para cada novo número inteiro digitado pelo usuário no teclado, o programa 
 * deve alocar uma nova variável no heap e inserí-la no final da lista composta 
 * pelos números já digitados. Quando o usuário digitar um número menor ou igual 
 * a 0 (zero), o programa deve imprimir todos os números digitados, na ordem em 
 * que eles foram alimentados pelo usuário. Utilizar o tipo de dados struct Item 
 * definido abaixo:

    struct Item {
        int valor;
        struct Item *proximo;
    };
    
 */ 

#include <iostream>

 int main () {

    return 0;
 }