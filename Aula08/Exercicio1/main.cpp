/*
  Escreva uma função com a seguinte interface:
  int soma( int (*f)(int), int inicio, int fim );
  Uma chamada soma( g, i, j ) deve retornar g( i ) + g( i + 1 ) + ... + g( j ). 
  Faça diversas dessas chamadas em uma função main() e imprima o resultado de cada uma delas.
 */
  
#include <iostream>

int main () {
  
  return 0;
}